<?php 

	require '../registration/db.php';

	  $json = json_decode($_POST["upload"]);
	  var_dump($json);

	  // $gids = R::dispense('gids');
   //    $gids->name = $data[''];

   //    $user->password = password_hash($data['password'], PASSWORD_DEFAULT); //пароль 
   //    R::store($user);


 ?>